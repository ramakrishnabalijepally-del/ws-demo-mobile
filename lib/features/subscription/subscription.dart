/// Subscription — design system Pattern D, value then payment.
///
/// Reached only by touching a locked feature. It says what stays free before it
/// says a price, and the comparison matrix marks absence with an em dash
/// because the product does not punish the cheaper choice visually.
library;

export 'presentation/screens/subscription_screens.dart';
